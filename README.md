Be sure to change the user name in modules/users.nix to the user name desired. Aditionally, check boot for any required input for luks Encryption, and use your installed luks configuration.

Nvidia is disabled by default
